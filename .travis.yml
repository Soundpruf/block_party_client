language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
script:
  - CI=false npm run build
  - npm test
deploy:
  provider: heroku
  app:
    master: block-party-client
    staging: block-party-staging-client